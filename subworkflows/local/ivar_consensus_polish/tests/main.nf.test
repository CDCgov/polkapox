nextflow_workflow {

    name "Test subworkflow IVAR_CONSENSUS_POLISH"
    script "../main.nf"
    workflow "IVAR_CONSENSUS_POLISH"

    test("Should run without failures") {

        when {
            params {
            }
            workflow {
                """
                input[0] = Channel.of([
                    [ id: 'test_sub' ],
                    file("test_sub.bam", checkIfExists: true),
                    file("test_sub.assembly_asm.fasta", checkIfExists: true)
                ])
                input[1] = false
                """
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out).match()
        }

    }

}
