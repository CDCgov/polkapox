nextflow_process {

    name "Test Process SAMPLESHEET_CHECK"
    script "../main.nf"
    process "SAMPLESHEET_CHECK"

    test("Validate samplesheet") {

        when {
            params {

            }
            process {
                """
                 input[0] = file("${moduleTestDir}/samplesheet.test.csv")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
