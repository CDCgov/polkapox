nextflow_process {

    name "Test Process SUMMARIZE_QC"
    script "modules/local/summarize_qc/summarize_qc.nf"
    process "SUMMARIZE_QC"

    test("Summarize QC Test") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                 input[0] = file("${projectDir}/tests/modules/local/summarize_qc/")
                 input[1] = file("${projectDir}/assets/samplesheet.test.csv")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
