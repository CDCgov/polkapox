nextflow_process {

    name "Test Process MUMMER"
    script "modules/nf-core/mummer/main.nf"
    process "MUMMER"

    test("Mummer Test") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = Channel.of([
                    [ id: 'test_sub'],
                    [
                        file("${projectDir}/tests/nf-test-data/test_sub.assembly_asm.fasta", checkIfExists: true)
                    ], 
                    [
                        file("${projectDir}/tests/nf-test-data/test_sub.final.fa", checkIfExists: true)
                    ]
                ])
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
