nextflow_process {

    name "Test Process UNICYCLER"
    script "modules/nf-core/unicycler/main.nf"
    process "UNICYCLER"

    test("Unicycler Test") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = Channel.of([
                    [ id: 'test_sub', single_end: false ],
                    [
                        file("${projectDir}/tests/nf-test-data/test_sub_1.fastp.fastq.gz", checkIfExists: true),
                        file("${projectDir}/tests/nf-test-data/test_sub_2.fastp.fastq.gz", checkIfExists: true)
                    ]
                ])
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
