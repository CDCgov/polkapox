nextflow_process {

    name "Test Process SEQTK_SUBSEQ"
    script "modules/nf-core/seqtk/subseq/main.nf"
    process "SEQTK_SUBSEQ"

    test("SEQTK Test") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [
                        [meta: 'test_sub'], 
                        [file('${projectDir}/tests/nf-test-data/test_sub_1.fq.gz'), file('${projectDir}/tests/nf-test-data/test_sub_2.fq.gz')]
                        ]
                input[1] = [ [meta: 'test_sub'], '${projectDir}/tests/nf-test-data/test_sub.kraken2.classifiedreads.txt' ]
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
