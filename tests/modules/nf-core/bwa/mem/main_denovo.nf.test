nextflow_process {

    name "Test Process BWA_MEM"
    script "modules/nf-core/bwa/mem/main_denovo.nf"
    process "BWA_MEM"

    test("BWA Mem Denovo Test") {

        when {
            params {

            }
            process {
                """
                // define inputs of the process here. Example:
                 input[0] = [
                [id: 'test_sub'],
                file("${projectDir}/assets/NC_003310_clade_I.fasta"),
                [file("${projectDir}/tests/nf-test-data/test_sub_1.fastp.fastq.gz"),
                 file("${projectDir}/tests/nf-test-data/test_sub_2.fastp.fastq.gz")]
            ]
                 input[1] = file("${projectDir}/tests/nf-test-data/test_sub.bam")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
