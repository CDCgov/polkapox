nextflow_process {

    name "Test Process BWA_MEM"
    script "modules/nf-core/bwa/mem/main.nf"
    process "BWA_MEM"

    test("BWA Mem Test") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                 input[0] = [
                [id: 'test_sub'],
                [file("${projectDir}/tests/nf-test-data/test_sub_1.fastp.fastq.gz"),
                 file("${projectDir}/tests/nf-test-data/test_sub_2.fastp.fastq.gz")]
            ]
                 input[1] = file("${projectDir}/tests/nf-test-data/test_sub.bam.bai")
                 input[2] = true
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
