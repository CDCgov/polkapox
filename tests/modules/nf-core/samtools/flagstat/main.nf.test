nextflow_process {

    name "Test Process SAMTOOLS_FLAGSTAT"
    script "modules/nf-core/samtools/flagstat/main.nf"
    process "SAMTOOLS_FLAGSTAT"

    test("Samtools Flagstat") {

        when {
            params {

            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = [ [ meta: 'test_sub'], file("${projectDir}/tests/nf-test-data/test_sub.bam"), file("${projectDir}/tests/nf-test-data/test_sub.bam.bai") ]

                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
