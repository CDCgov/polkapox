nextflow_process {

    name "Test Process QUAST"
    script "modules/nf-core/quast/main.nf"
    process "QUAST"

    test("Quast Test") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = Channel.of(
                    file("${projectDir}/tests/nf-test-data/test_sub.contigs.fasta", checkIfExists: true)
                )
                input[1] = true
                input[2] = true
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
