nextflow_process {

    name "Test Process IVAR_VARIANTS"
    script "modules/nf-core/ivar/variants/main.nf"
    process "IVAR_VARIANTS"

    test("Ivar Variants Test") {

        when {
            params {
            }
            process {
                """
                input[0] = Channel.of([
                        [ id: 'test_sub'],
                        [
                            file("${projectDir}/tests/nf-test-data/test_sub.bam", checkIfExists: true)
                        ]
                    ])
                input[1] = true
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
