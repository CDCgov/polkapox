/*
========================================================================================
    Nextflow config file for running tests
========================================================================================
*/

process {
    // Override the pipeline-wide 'ignore' error strategy so that GRAPH_RECON
    // failure tests correctly surface a non-zero exit to nf-test.
    withName: 'GRAPH_RECON' {
        errorStrategy = 'terminate'
    }
}
